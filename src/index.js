import equals from "./lib/equals.js";
import escape from "./lib/escape.js";
import contains from "./lib/contaions.js";

const version = "1.0.0";

const validator = {
  equals,
  escape,
  contains,
};

export default validator;
